# @inventory/api

NestJS backend of the Software Inventory Tool. REST API under `/api/v1`.

Run everything from the repo root — see the root [README](../../README.md) and [CLAUDE.md](../../CLAUDE.md) for commands (`npm run dev`, `npm run test`, `npm run test:e2e`, `npm run lint`).
